#include <stdio.h>

//This will get the size of a grid of numbers in a txt file.
int * fillArray(int xsize, int ysize, int elementSize, FILE *fp)
{
	int toFill[xsize][ysize], xpos, ypos, current;

	for(ypos = 0; ypos <= ysize; ypos++)
	{
		for(xpos = 0; xpos <= xsize; xpos++)
		{
			//This part needs to convert a something like 08 to and int of 8
			
		}
	}
	return toFill;
}
